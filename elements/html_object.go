// Code generated by speckles. DO NOT EDIT.
// Source: generator/templates/element.go.tmpl

// Package elements html object is generated from configuration file.
package elements

import (
	"fmt"
	"sort"
	"strings"

	"github.com/igrmk/treemap/v2"
)

// The HTML <object> element represents an external resource, which can be
// treated as an image, a nested browsing context, or a resource to be handled
// by a plugin.
type ObjectElement struct {
	*Element
}

// Create a new ObjectElement element. This will create a new element
// with the tag "object" during rendering.
func Object(children ...ElementRenderer) *ObjectElement {
	e := NewElement("object", children...)
	e.IsSelfClosing = false
	e.Descendants = children
	return &ObjectElement{Element: e}
}

func (e *ObjectElement) Children(children ...ElementRenderer) *ObjectElement {
	e.Descendants = append(e.Descendants, children...)
	return e
}

func (e *ObjectElement) IfChildren(condition bool, children ...ElementRenderer) *ObjectElement {
	if condition {
		e.Descendants = append(e.Descendants, children...)
	}
	return e
}

func (e *ObjectElement) TernChildren(condition bool, trueChildren, falseChildren ElementRenderer) *ObjectElement {
	if condition {
		e.Descendants = append(e.Descendants, trueChildren)
	} else {
		e.Descendants = append(e.Descendants, falseChildren)
	}
	return e
}

func (e *ObjectElement) BoolAttr(name string) *ObjectElement {
	if e.BoolAttributes == nil {
		e.BoolAttributes = treemap.New[string, bool]()
	}
	e.BoolAttributes.Set(name, true)
	return e
}

func (e *ObjectElement) IfBoolAttr(condition bool, name string) *ObjectElement {
	if condition {
		e.BoolAttr(name)
	}
	return e
}

func (e *ObjectElement) Attr(name, value string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set(name, value)
	return e
}

func (e *ObjectElement) IfAttr(condition bool, name, value string) *ObjectElement {
	if condition {
		e.Attr(name, value)
	}
	return e
}

func (e *ObjectElement) Text(text string) *ObjectElement {
	e.Descendants = append(e.Descendants, Text(text))
	return e
}

func (e *ObjectElement) TextF(format string, args ...any) *ObjectElement {
	return e.Text(fmt.Sprintf(format, args...))
}

func (e *ObjectElement) IfText(condition bool, text string) *ObjectElement {
	if condition {
		e.Descendants = append(e.Descendants, Text(text))
	}
	return e
}

func (e *ObjectElement) IfTextF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Descendants = append(e.Descendants, Text(fmt.Sprintf(format, args...)))
	}
	return e
}

func (e *ObjectElement) Escaped(text string) *ObjectElement {
	e.Descendants = append(e.Descendants, Escaped(text))
	return e
}

func (e *ObjectElement) IfEscaped(condition bool, text string) *ObjectElement {
	if condition {
		e.Descendants = append(e.Descendants, Escaped(text))
	}
	return e
}

func (e *ObjectElement) EscapedF(format string, args ...any) *ObjectElement {
	return e.Escaped(fmt.Sprintf(format, args...))
}

func (e *ObjectElement) IfEscapedF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Descendants = append(e.Descendants, EscapedF(format, args...))
	}
	return e
}

// Address of the resource.
func (e *ObjectElement) Data(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("data", s)
	return e
}

// Address of the resource.
func (e *ObjectElement) DataF(format string, args ...any) *ObjectElement {
	return e.Data(fmt.Sprintf(format, args...))
}

// Address of the resource.
func (e *ObjectElement) IfData(condition bool, s string) *ObjectElement {
	if condition {
		e.Data(s)
	}
	return e
}

// Address of the resource.
func (e *ObjectElement) IfDataF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Data(fmt.Sprintf(format, args...))
	}
	return e
}

// Address of the resource.
// Remove the attribute Data from the element.
func (e *ObjectElement) DataRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("data")
	return e
}

// Associates the control with a form element.
func (e *ObjectElement) Form(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("form", s)
	return e
}

// Associates the control with a form element.
func (e *ObjectElement) FormF(format string, args ...any) *ObjectElement {
	return e.Form(fmt.Sprintf(format, args...))
}

// Associates the control with a form element.
func (e *ObjectElement) IfForm(condition bool, s string) *ObjectElement {
	if condition {
		e.Form(s)
	}
	return e
}

// Associates the control with a form element.
func (e *ObjectElement) IfFormF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Form(fmt.Sprintf(format, args...))
	}
	return e
}

// Associates the control with a form element.
// Remove the attribute Form from the element.
func (e *ObjectElement) FormRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("form")
	return e
}

// Height of the element.
func (e *ObjectElement) Height(i int) *ObjectElement {
	if e.IntAttributes == nil {
		e.IntAttributes = treemap.New[string, int]()
	}
	e.IntAttributes.Set("height", i)
	return e
}

// Height of the element.
func (e *ObjectElement) IfHeight(condition bool, i int) *ObjectElement {
	if condition {
		e.Height(i)
	}
	return e
}

// Height of the element.
// Remove the attribute Height from the element.
func (e *ObjectElement) HeightRemove() *ObjectElement {
	if e.IntAttributes == nil {
		return e
	}
	e.IntAttributes.Del("height")
	return e
}

// Name of the element to use for form submission and in the form.elements API.
func (e *ObjectElement) Name(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("name", s)
	return e
}

// Name of the element to use for form submission and in the form.elements API.
func (e *ObjectElement) NameF(format string, args ...any) *ObjectElement {
	return e.Name(fmt.Sprintf(format, args...))
}

// Name of the element to use for form submission and in the form.elements API.
func (e *ObjectElement) IfName(condition bool, s string) *ObjectElement {
	if condition {
		e.Name(s)
	}
	return e
}

// Name of the element to use for form submission and in the form.elements API.
func (e *ObjectElement) IfNameF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Name(fmt.Sprintf(format, args...))
	}
	return e
}

// Name of the element to use for form submission and in the form.elements API.
// Remove the attribute Name from the element.
func (e *ObjectElement) NameRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("name")
	return e
}

// Type of embedded resource.
func (e *ObjectElement) Type(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("type", s)
	return e
}

// Type of embedded resource.
func (e *ObjectElement) TypeF(format string, args ...any) *ObjectElement {
	return e.Type(fmt.Sprintf(format, args...))
}

// Type of embedded resource.
func (e *ObjectElement) IfType(condition bool, s string) *ObjectElement {
	if condition {
		e.Type(s)
	}
	return e
}

// Type of embedded resource.
func (e *ObjectElement) IfTypeF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Type(fmt.Sprintf(format, args...))
	}
	return e
}

// Type of embedded resource.
// Remove the attribute Type from the element.
func (e *ObjectElement) TypeRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("type")
	return e
}

// Whether the type attribute and the Content-Type value need to match for the
// resource to be used.
func (e *ObjectElement) Typemustmatch() *ObjectElement {
	e.TypemustmatchSet(true)
	return e
}

// Whether the type attribute and the Content-Type value need to match for the
// resource to be used.
func (e *ObjectElement) IfTypemustmatch(condition bool) *ObjectElement {
	if condition {
		e.TypemustmatchSet(true)
	}
	return e
}

// Whether the type attribute and the Content-Type value need to match for the
// resource to be used.
// Set the attribute Typemustmatch to the value b explicitly.
func (e *ObjectElement) TypemustmatchSet(b bool) *ObjectElement {
	if e.BoolAttributes == nil {
		e.BoolAttributes = treemap.New[string, bool]()
	}
	e.BoolAttributes.Set("typemustmatch", b)
	return e
}

// Whether the type attribute and the Content-Type value need to match for the
// resource to be used.
func (e *ObjectElement) IfSetTypemustmatch(condition bool, b bool) *ObjectElement {
	if condition {
		e.TypemustmatchSet(b)
	}
	return e
}

// Remove the attribute Typemustmatch from the element.
// Whether the type attribute and the Content-Type value need to match for the
// resource to be used.
func (e *ObjectElement) TypemustmatchRemove() *ObjectElement {
	if e.BoolAttributes == nil {
		return e
	}
	e.BoolAttributes.Del("typemustmatch")
	return e
}

// Name of image map to use.
func (e *ObjectElement) Usemap(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("usemap", s)
	return e
}

// Name of image map to use.
func (e *ObjectElement) UsemapF(format string, args ...any) *ObjectElement {
	return e.Usemap(fmt.Sprintf(format, args...))
}

// Name of image map to use.
func (e *ObjectElement) IfUsemap(condition bool, s string) *ObjectElement {
	if condition {
		e.Usemap(s)
	}
	return e
}

// Name of image map to use.
func (e *ObjectElement) IfUsemapF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Usemap(fmt.Sprintf(format, args...))
	}
	return e
}

// Name of image map to use.
// Remove the attribute Usemap from the element.
func (e *ObjectElement) UsemapRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("usemap")
	return e
}

// Width of the element.
func (e *ObjectElement) Width(i int) *ObjectElement {
	if e.IntAttributes == nil {
		e.IntAttributes = treemap.New[string, int]()
	}
	e.IntAttributes.Set("width", i)
	return e
}

// Width of the element.
func (e *ObjectElement) IfWidth(condition bool, i int) *ObjectElement {
	if condition {
		e.Width(i)
	}
	return e
}

// Width of the element.
// Remove the attribute Width from the element.
func (e *ObjectElement) WidthRemove() *ObjectElement {
	if e.IntAttributes == nil {
		return e
	}
	e.IntAttributes.Del("width")
	return e
}

// The accesskey global attribute provides a hint for generating a keyboard
// shortcut for the current element. The attribute value must consist of a
// single printable character (which includes accented and other characters that
// can be generated by the keyboard).
func (e *ObjectElement) Accesskey(r rune) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("accesskey", string(r))
	return e
}

// The accesskey global attribute provides a hint for generating a keyboard
// shortcut for the current element. The attribute value must consist of a
// single printable character (which includes accented and other characters that
// can be generated by the keyboard).
func (e *ObjectElement) IfAccesskey(condition bool, r rune) *ObjectElement {
	if condition {
		e.Accesskey(r)
	}
	return e
}

// The accesskey global attribute provides a hint for generating a keyboard
// shortcut for the current element. The attribute value must consist of a
// single printable character (which includes accented and other characters that
// can be generated by the keyboard).
// Remove the attribute Accesskey from the element.
func (e *ObjectElement) AccesskeyRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("accesskey")
	return e
}

// The autocapitalize global attribute is an enumerated attribute that controls
// whether and how text input is automatically capitalized as it is
// entered/edited by the user. autocapitalize can be set on <input> and
// <textarea> elements, and on their containing <form> elements. When
// autocapitalize is set on a <form> element, it sets the autocapitalize
// behavior for all contained <input>s and <textarea>s, overriding any
// autocapitalize values set on contained elements. autocapitalize has no effect
// on the url, email, or password <input> types, where autocapitalization is
// never enabled. Where autocapitalize is not specified, the adopted default
// behavior varies between browsers. For example: Chrome and Safari default to
// on/sentences Firefox defaults to off/none.
func (e *ObjectElement) Autocapitalize(c ObjectAutocapitalizeChoice) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("autocapitalize", string(c))
	return e
}

type ObjectAutocapitalizeChoice string

const (
	// Do not automatically capitalize any text.
	ObjectAutocapitalizeOff ObjectAutocapitalizeChoice = "off"
	// Do not automatically capitalize any text.
	ObjectAutocapitalizeNone ObjectAutocapitalizeChoice = "none"
	// Automatically capitalize the first character of each sentence.
	ObjectAutocapitalizeSentences ObjectAutocapitalizeChoice = "sentences"
	// Automatically capitalize the first character of each sentence.
	ObjectAutocapitalizeOn ObjectAutocapitalizeChoice = "on"
	// Automatically capitalize the first character of each word.
	ObjectAutocapitalizeWords ObjectAutocapitalizeChoice = "words"
	// Automatically capitalize all characters.
	ObjectAutocapitalizeCharacters ObjectAutocapitalizeChoice = "characters"
)

// The autocapitalize global attribute is an enumerated attribute that controls
// whether and how text input is automatically capitalized as it is
// entered/edited by the user. autocapitalize can be set on <input> and
// <textarea> elements, and on their containing <form> elements. When
// autocapitalize is set on a <form> element, it sets the autocapitalize
// behavior for all contained <input>s and <textarea>s, overriding any
// autocapitalize values set on contained elements. autocapitalize has no effect
// on the url, email, or password <input> types, where autocapitalization is
// never enabled. Where autocapitalize is not specified, the adopted default
// behavior varies between browsers. For example: Chrome and Safari default to
// on/sentences Firefox defaults to off/none.
// Remove the attribute Autocapitalize from the element.
func (e *ObjectElement) AutocapitalizeRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("autocapitalize")
	return e
}

// The autofocus global attribute is a Boolean attribute indicating that an
// element should be focused on page load, or when the <dialog> that it is part
// of is displayed.
// Accessibility concerns Automatically focusing a form control can confuse
// visually-impaired people using screen-reading technology and people with
// cognitive impairments. When autofocus is assigned, screen-readers "teleport"
// their user to the form control without warning them beforehand.
// Use careful consideration for accessibility when applying the autofocus
// attribute. Automatically focusing on a control can cause the page to scroll
// on load. The focus can also cause dynamic keyboards to display on some touch
// devices. While a screen reader will announce the label of the form control
// receiving focus, the screen reader will not announce anything before the
// label, and the sighted user on a small device will equally miss the context
// created by the preceding content.
func (e *ObjectElement) Autofocus() *ObjectElement {
	e.AutofocusSet(true)
	return e
}

// The autofocus global attribute is a Boolean attribute indicating that an
// element should be focused on page load, or when the <dialog> that it is part
// of is displayed.
// Accessibility concerns Automatically focusing a form control can confuse
// visually-impaired people using screen-reading technology and people with
// cognitive impairments. When autofocus is assigned, screen-readers "teleport"
// their user to the form control without warning them beforehand.
// Use careful consideration for accessibility when applying the autofocus
// attribute. Automatically focusing on a control can cause the page to scroll
// on load. The focus can also cause dynamic keyboards to display on some touch
// devices. While a screen reader will announce the label of the form control
// receiving focus, the screen reader will not announce anything before the
// label, and the sighted user on a small device will equally miss the context
// created by the preceding content.
func (e *ObjectElement) IfAutofocus(condition bool) *ObjectElement {
	if condition {
		e.AutofocusSet(true)
	}
	return e
}

// The autofocus global attribute is a Boolean attribute indicating that an
// element should be focused on page load, or when the <dialog> that it is part
// of is displayed.
// Accessibility concerns Automatically focusing a form control can confuse
// visually-impaired people using screen-reading technology and people with
// cognitive impairments. When autofocus is assigned, screen-readers "teleport"
// their user to the form control without warning them beforehand.
// Use careful consideration for accessibility when applying the autofocus
// attribute. Automatically focusing on a control can cause the page to scroll
// on load. The focus can also cause dynamic keyboards to display on some touch
// devices. While a screen reader will announce the label of the form control
// receiving focus, the screen reader will not announce anything before the
// label, and the sighted user on a small device will equally miss the context
// created by the preceding content.
// Set the attribute Autofocus to the value b explicitly.
func (e *ObjectElement) AutofocusSet(b bool) *ObjectElement {
	if e.BoolAttributes == nil {
		e.BoolAttributes = treemap.New[string, bool]()
	}
	e.BoolAttributes.Set("autofocus", b)
	return e
}

// The autofocus global attribute is a Boolean attribute indicating that an
// element should be focused on page load, or when the <dialog> that it is part
// of is displayed.
// Accessibility concerns Automatically focusing a form control can confuse
// visually-impaired people using screen-reading technology and people with
// cognitive impairments. When autofocus is assigned, screen-readers "teleport"
// their user to the form control without warning them beforehand.
// Use careful consideration for accessibility when applying the autofocus
// attribute. Automatically focusing on a control can cause the page to scroll
// on load. The focus can also cause dynamic keyboards to display on some touch
// devices. While a screen reader will announce the label of the form control
// receiving focus, the screen reader will not announce anything before the
// label, and the sighted user on a small device will equally miss the context
// created by the preceding content.
func (e *ObjectElement) IfSetAutofocus(condition bool, b bool) *ObjectElement {
	if condition {
		e.AutofocusSet(b)
	}
	return e
}

// Remove the attribute Autofocus from the element.
// The autofocus global attribute is a Boolean attribute indicating that an
// element should be focused on page load, or when the <dialog> that it is part
// of is displayed.
// Accessibility concerns Automatically focusing a form control can confuse
// visually-impaired people using screen-reading technology and people with
// cognitive impairments. When autofocus is assigned, screen-readers "teleport"
// their user to the form control without warning them beforehand.
// Use careful consideration for accessibility when applying the autofocus
// attribute. Automatically focusing on a control can cause the page to scroll
// on load. The focus can also cause dynamic keyboards to display on some touch
// devices. While a screen reader will announce the label of the form control
// receiving focus, the screen reader will not announce anything before the
// label, and the sighted user on a small device will equally miss the context
// created by the preceding content.
func (e *ObjectElement) AutofocusRemove() *ObjectElement {
	if e.BoolAttributes == nil {
		return e
	}
	e.BoolAttributes.Del("autofocus")
	return e
}

// The class global attribute is a space-separated list of the case-sensitive
// classes of the element. Classes allow CSS and JavaScript to select and access
// specific elements via the class selectors or functions like the DOM method
// document.getElementsByClassName.
func (e *ObjectElement) Class(s string) *ObjectElement {
	values := strings.Split(s, " ")
	if e.DelimitedStrings == nil {
		e.DelimitedStrings = treemap.New[string, *DelimitedBuilder[string]]()
	}
	ds, ok := e.DelimitedStrings.Get("class")
	if !ok {
		ds = NewDelimitedBuilder[string](" ")
		e.DelimitedStrings.Set("class", ds)
	}
	ds.Add(values...)
	return e
}

// The class global attribute is a space-separated list of the case-sensitive
// classes of the element. Classes allow CSS and JavaScript to select and access
// specific elements via the class selectors or functions like the DOM method
// document.getElementsByClassName.
func (e *ObjectElement) IfClass(condition bool, s string) *ObjectElement {
	if condition {
		e.Class(s)
	}
	return e
}

// The class global attribute is a space-separated list of the case-sensitive
// classes of the element. Classes allow CSS and JavaScript to select and access
// specific elements via the class selectors or functions like the DOM method
// document.getElementsByClassName.
// Remove the values from the attribute Class in the element.
func (e *ObjectElement) ClassRemove(s ...string) *ObjectElement {
	if e.DelimitedStrings == nil {
		return e
	}
	ds, ok := e.DelimitedStrings.Get("class")
	if !ok {
		return e
	}
	ds.Remove(s...)
	return e
}

// The contenteditable global attribute is an enumerated attribute indicating if
// the element should be editable by the user. If so, the browser modifies its
// widget to allow editing.
func (e *ObjectElement) Contenteditable(c ObjectContenteditableChoice) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("contenteditable", string(c))
	return e
}

type ObjectContenteditableChoice string

const (
	// The element is editable.
	ObjectContenteditableEmpty ObjectContenteditableChoice = ""
	// The element is editable.
	ObjectContenteditableTrue ObjectContenteditableChoice = "true"
	// The element is not editable.
	ObjectContenteditableFalse ObjectContenteditableChoice = "false"
	// which indicates that the element's raw text is editable, but rich text
	// formatting is disabled.
	ObjectContenteditablePlaintextOnly ObjectContenteditableChoice = "plaintext-only"
)

// The contenteditable global attribute is an enumerated attribute indicating if
// the element should be editable by the user. If so, the browser modifies its
// widget to allow editing.
// Remove the attribute Contenteditable from the element.
func (e *ObjectElement) ContenteditableRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("contenteditable")
	return e
}

// The dir global attribute is an enumerated attribute that indicates the
// directionality of the element's text. Note: This attribute is mandatory for
// the <bdo> element where it has a different semantic meaning. This attribute
// is not inherited by the <bdi> element. If not set, its value is auto. This
// attribute can be overridden by the CSS properties direction and unicode-bidi,
// if a CSS page is active and the element supports these properties. As the
// directionality of the text is semantically related to its content and not to
// its presentation, it is recommended that web developers use this attribute
// instead of the related CSS properties when possible. That way, the text will
// display correctly even on a browser that doesn't support CSS or has the CSS
// deactivated. The auto value should be used for data with an unknown
// directionality, like data coming from user input, eventually stored in a
// database.
func (e *ObjectElement) Dir(c ObjectDirChoice) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("dir", string(c))
	return e
}

type ObjectDirChoice string

const (
	// which means left to right and is to be used for languages that are written
	// from the left to the right (like English);
	ObjectDirLtr ObjectDirChoice = "ltr"
	// which means right to left and is to be used for languages that are written
	// from the right to the left (like Arabic);
	ObjectDirRtl ObjectDirChoice = "rtl"
	// which lets the user agent decide. It uses a basic algorithm as it parses the
	// characters inside the element until it finds a character with a strong
	// directionality, then it applies that directionality to the whole element.
	ObjectDirAuto ObjectDirChoice = "auto"
)

// The dir global attribute is an enumerated attribute that indicates the
// directionality of the element's text. Note: This attribute is mandatory for
// the <bdo> element where it has a different semantic meaning. This attribute
// is not inherited by the <bdi> element. If not set, its value is auto. This
// attribute can be overridden by the CSS properties direction and unicode-bidi,
// if a CSS page is active and the element supports these properties. As the
// directionality of the text is semantically related to its content and not to
// its presentation, it is recommended that web developers use this attribute
// instead of the related CSS properties when possible. That way, the text will
// display correctly even on a browser that doesn't support CSS or has the CSS
// deactivated. The auto value should be used for data with an unknown
// directionality, like data coming from user input, eventually stored in a
// database.
// Remove the attribute Dir from the element.
func (e *ObjectElement) DirRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("dir")
	return e
}

// The draggable global attribute is an enumerated attribute that indicates
// whether the element can be dragged, either with native browser behavior or
// the HTML Drag and Drop API.
func (e *ObjectElement) Draggable(c ObjectDraggableChoice) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("draggable", string(c))
	return e
}

type ObjectDraggableChoice string

const (
	// The element is draggable.
	ObjectDraggableTrue ObjectDraggableChoice = "true"
	// The element is not draggable.
	ObjectDraggableFalse ObjectDraggableChoice = "false"
	// drag behavior is the default browser behavior: only text selections, images,
	// and links can be dragged. For other elements, the event ondragstart must be
	// set for drag and drop to work
	ObjectDraggableEmpty ObjectDraggableChoice = ""
	// drag behavior is the default browser behavior: only text selections, images,
	// and links can be dragged. For other elements, the event ondragstart must be
	// set for drag and drop to work
	ObjectDraggableAuto ObjectDraggableChoice = "auto"
)

// The draggable global attribute is an enumerated attribute that indicates
// whether the element can be dragged, either with native browser behavior or
// the HTML Drag and Drop API.
// Remove the attribute Draggable from the element.
func (e *ObjectElement) DraggableRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("draggable")
	return e
}

// The enterkeyhint global attribute is an enumerated attribute defining what
// action label (or icon) to present for the enter key on virtual keyboards.
func (e *ObjectElement) Enterkeyhint(c ObjectEnterkeyhintChoice) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("enterkeyhint", string(c))
	return e
}

type ObjectEnterkeyhintChoice string

const (
	// Typically inserting a new line.
	ObjectEnterkeyhintEnter ObjectEnterkeyhintChoice = "enter"
	// Typically meaning there is nothing more to input and the input method editor
	// (IME) will be closed.
	ObjectEnterkeyhintDone ObjectEnterkeyhintChoice = "done"
	// Typically meaning to take the user to the target of the text they typed.
	ObjectEnterkeyhintGo ObjectEnterkeyhintChoice = "go"
	// Typically meaning to take the user to the next field that will accept text.
	ObjectEnterkeyhintNext ObjectEnterkeyhintChoice = "next"
	// Typically meaning to take the user to the previous field that will accept
	// text.
	ObjectEnterkeyhintPrevious ObjectEnterkeyhintChoice = "previous"
	// Typically taking the user to the results of searching for the text they have
	// typed.
	ObjectEnterkeyhintSearch ObjectEnterkeyhintChoice = "search"
	// Typically delivering the text to its target.
	ObjectEnterkeyhintSend ObjectEnterkeyhintChoice = "send"
)

// The enterkeyhint global attribute is an enumerated attribute defining what
// action label (or icon) to present for the enter key on virtual keyboards.
// Remove the attribute Enterkeyhint from the element.
func (e *ObjectElement) EnterkeyhintRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("enterkeyhint")
	return e
}

// The exportparts global attribute allows you to select and style elements
// existing in nested shadow trees, by exporting their part names. The shadow
// tree is an isolated structure where identifiers, classes, and styles cannot
// be reached by selectors or queries belonging to a regular DOM. To apply a
// style to an element living in a shadow tree, by CSS rule created outside of
// it, part global attribute has to be used. It has to be assigned to an element
// present in Shadow Tree, and its value should be some identifier. Rules
// present outside of the shadow tree, must use the ::part pseudo-element,
// containing the same identifier as the argument. The global attribute part
// makes the element visible on just a single level of depth. When the shadow
// tree is nested, parts will be visible only to the parent of the shadow tree
// but not to its ancestor. Exporting parts further down is exactly what
// exportparts attribute is for. Attribute exportparts must be placed on a
// shadow Host, which is the element to which the shadow tree is attached. The
// value of the attribute should be a comma-separated list of part names present
// in the shadow tree and which should be made available via a DOM outside of
// the current structure.
func (e *ObjectElement) Exportparts(s string) *ObjectElement {
	values := strings.Split(s, ",")
	if e.DelimitedStrings == nil {
		e.DelimitedStrings = treemap.New[string, *DelimitedBuilder[string]]()
	}
	ds, ok := e.DelimitedStrings.Get("exportparts")
	if !ok {
		ds = NewDelimitedBuilder[string](",")
		e.DelimitedStrings.Set("exportparts", ds)
	}
	ds.Add(values...)
	return e
}

// The exportparts global attribute allows you to select and style elements
// existing in nested shadow trees, by exporting their part names. The shadow
// tree is an isolated structure where identifiers, classes, and styles cannot
// be reached by selectors or queries belonging to a regular DOM. To apply a
// style to an element living in a shadow tree, by CSS rule created outside of
// it, part global attribute has to be used. It has to be assigned to an element
// present in Shadow Tree, and its value should be some identifier. Rules
// present outside of the shadow tree, must use the ::part pseudo-element,
// containing the same identifier as the argument. The global attribute part
// makes the element visible on just a single level of depth. When the shadow
// tree is nested, parts will be visible only to the parent of the shadow tree
// but not to its ancestor. Exporting parts further down is exactly what
// exportparts attribute is for. Attribute exportparts must be placed on a
// shadow Host, which is the element to which the shadow tree is attached. The
// value of the attribute should be a comma-separated list of part names present
// in the shadow tree and which should be made available via a DOM outside of
// the current structure.
func (e *ObjectElement) IfExportparts(condition bool, s string) *ObjectElement {
	if condition {
		e.Exportparts(s)
	}
	return e
}

// The exportparts global attribute allows you to select and style elements
// existing in nested shadow trees, by exporting their part names. The shadow
// tree is an isolated structure where identifiers, classes, and styles cannot
// be reached by selectors or queries belonging to a regular DOM. To apply a
// style to an element living in a shadow tree, by CSS rule created outside of
// it, part global attribute has to be used. It has to be assigned to an element
// present in Shadow Tree, and its value should be some identifier. Rules
// present outside of the shadow tree, must use the ::part pseudo-element,
// containing the same identifier as the argument. The global attribute part
// makes the element visible on just a single level of depth. When the shadow
// tree is nested, parts will be visible only to the parent of the shadow tree
// but not to its ancestor. Exporting parts further down is exactly what
// exportparts attribute is for. Attribute exportparts must be placed on a
// shadow Host, which is the element to which the shadow tree is attached. The
// value of the attribute should be a comma-separated list of part names present
// in the shadow tree and which should be made available via a DOM outside of
// the current structure.
// Remove the values from the attribute Exportparts in the element.
func (e *ObjectElement) ExportpartsRemove(s ...string) *ObjectElement {
	if e.DelimitedStrings == nil {
		return e
	}
	ds, ok := e.DelimitedStrings.Get("exportparts")
	if !ok {
		return e
	}
	ds.Remove(s...)
	return e
}

// The hidden global attribute is a Boolean attribute indicating that the
// element is not yet, or is no longer, relevant. For example, it can be used to
// hide elements of the page that can't be used until the login process has been
// completed. Note that browsers typically implement hidden until found using
// content-visibility: hidden. This means that unlike elements in the hidden
// state, elements in the hidden until found state will have generated boxes,
// meaning that: the element will participate in page layout margin, borders,
// padding, and background for the element will be rendered. Also, the element
// needs to be affected by layout containment in order to be revealed. This
// means that if the element in the hidden until found state has a display value
// of none, contents, or inline, then the element will not be revealed by find
// in page or fragment navigation.
func (e *ObjectElement) Hidden(c ObjectHiddenChoice) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("hidden", string(c))
	return e
}

type ObjectHiddenChoice string

const (
	// set the element to the hidden state. Additionally, invalid values set the
	// element to the hidden state.
	ObjectHiddenEmpty ObjectHiddenChoice = ""
	// set the element to the hidden state. Additionally, invalid values set the
	// element to the hidden state.
	ObjectHiddenHidden ObjectHiddenChoice = "hidden"
	// the element is hidden but its content will be accessible to the browser's
	// "find in page" feature or to fragment navigation. When these features cause a
	// scroll to an element in a hidden until found subtree, the browser will fire a
	// beforematch event on the hidden element remove the hidden attribute from the
	// element scroll to the element
	//
	ObjectHiddenUntilFound ObjectHiddenChoice = "until-found"
)

// The hidden global attribute is a Boolean attribute indicating that the
// element is not yet, or is no longer, relevant. For example, it can be used to
// hide elements of the page that can't be used until the login process has been
// completed. Note that browsers typically implement hidden until found using
// content-visibility: hidden. This means that unlike elements in the hidden
// state, elements in the hidden until found state will have generated boxes,
// meaning that: the element will participate in page layout margin, borders,
// padding, and background for the element will be rendered. Also, the element
// needs to be affected by layout containment in order to be revealed. This
// means that if the element in the hidden until found state has a display value
// of none, contents, or inline, then the element will not be revealed by find
// in page or fragment navigation.
// Remove the attribute Hidden from the element.
func (e *ObjectElement) HiddenRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("hidden")
	return e
}

// The id global attribute defines a unique identifier (ID) which must be unique
// in the whole document. Its purpose is to identify the element when linking
// (using a fragment identifier), scripting, or styling (with CSS).
func (e *ObjectElement) ID(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("id", s)
	return e
}

// The id global attribute defines a unique identifier (ID) which must be unique
// in the whole document. Its purpose is to identify the element when linking
// (using a fragment identifier), scripting, or styling (with CSS).
func (e *ObjectElement) IDF(format string, args ...any) *ObjectElement {
	return e.ID(fmt.Sprintf(format, args...))
}

// The id global attribute defines a unique identifier (ID) which must be unique
// in the whole document. Its purpose is to identify the element when linking
// (using a fragment identifier), scripting, or styling (with CSS).
func (e *ObjectElement) IfID(condition bool, s string) *ObjectElement {
	if condition {
		e.ID(s)
	}
	return e
}

// The id global attribute defines a unique identifier (ID) which must be unique
// in the whole document. Its purpose is to identify the element when linking
// (using a fragment identifier), scripting, or styling (with CSS).
func (e *ObjectElement) IfIDF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.ID(fmt.Sprintf(format, args...))
	}
	return e
}

// The id global attribute defines a unique identifier (ID) which must be unique
// in the whole document. Its purpose is to identify the element when linking
// (using a fragment identifier), scripting, or styling (with CSS).
// Remove the attribute ID from the element.
func (e *ObjectElement) IDRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("id")
	return e
}

// The inert global attribute is a Boolean attribute indicating that the browser
// will ignore the element. With the inert attribute, all of the element's flat
// tree descendants (such as modal <dialog>s) that don't otherwise escape
// inertness are ignored. The inert attribute also makes the browser ignore
// input events sent by the user, including focus-related events and events from
// assistive technologies. Specifically, inert does the following: Prevents the
// click event from being fired when the user clicks on the element. Prevents
// the focus event from being raised by preventing the element from gaining
// focus. Hides the element and its content from assistive technologies by
// excluding them from the accessibility tree.
func (e *ObjectElement) Inert() *ObjectElement {
	e.InertSet(true)
	return e
}

// The inert global attribute is a Boolean attribute indicating that the browser
// will ignore the element. With the inert attribute, all of the element's flat
// tree descendants (such as modal <dialog>s) that don't otherwise escape
// inertness are ignored. The inert attribute also makes the browser ignore
// input events sent by the user, including focus-related events and events from
// assistive technologies. Specifically, inert does the following: Prevents the
// click event from being fired when the user clicks on the element. Prevents
// the focus event from being raised by preventing the element from gaining
// focus. Hides the element and its content from assistive technologies by
// excluding them from the accessibility tree.
func (e *ObjectElement) IfInert(condition bool) *ObjectElement {
	if condition {
		e.InertSet(true)
	}
	return e
}

// The inert global attribute is a Boolean attribute indicating that the browser
// will ignore the element. With the inert attribute, all of the element's flat
// tree descendants (such as modal <dialog>s) that don't otherwise escape
// inertness are ignored. The inert attribute also makes the browser ignore
// input events sent by the user, including focus-related events and events from
// assistive technologies. Specifically, inert does the following: Prevents the
// click event from being fired when the user clicks on the element. Prevents
// the focus event from being raised by preventing the element from gaining
// focus. Hides the element and its content from assistive technologies by
// excluding them from the accessibility tree.
// Set the attribute Inert to the value b explicitly.
func (e *ObjectElement) InertSet(b bool) *ObjectElement {
	if e.BoolAttributes == nil {
		e.BoolAttributes = treemap.New[string, bool]()
	}
	e.BoolAttributes.Set("inert", b)
	return e
}

// The inert global attribute is a Boolean attribute indicating that the browser
// will ignore the element. With the inert attribute, all of the element's flat
// tree descendants (such as modal <dialog>s) that don't otherwise escape
// inertness are ignored. The inert attribute also makes the browser ignore
// input events sent by the user, including focus-related events and events from
// assistive technologies. Specifically, inert does the following: Prevents the
// click event from being fired when the user clicks on the element. Prevents
// the focus event from being raised by preventing the element from gaining
// focus. Hides the element and its content from assistive technologies by
// excluding them from the accessibility tree.
func (e *ObjectElement) IfSetInert(condition bool, b bool) *ObjectElement {
	if condition {
		e.InertSet(b)
	}
	return e
}

// Remove the attribute Inert from the element.
// The inert global attribute is a Boolean attribute indicating that the browser
// will ignore the element. With the inert attribute, all of the element's flat
// tree descendants (such as modal <dialog>s) that don't otherwise escape
// inertness are ignored. The inert attribute also makes the browser ignore
// input events sent by the user, including focus-related events and events from
// assistive technologies. Specifically, inert does the following: Prevents the
// click event from being fired when the user clicks on the element. Prevents
// the focus event from being raised by preventing the element from gaining
// focus. Hides the element and its content from assistive technologies by
// excluding them from the accessibility tree.
func (e *ObjectElement) InertRemove() *ObjectElement {
	if e.BoolAttributes == nil {
		return e
	}
	e.BoolAttributes.Del("inert")
	return e
}

// The inputmode global attribute is an enumerated attribute that hints at the
// type of data that might be entered by the user while editing the element or
// its contents. This allows a browser to display an appropriate virtual
// keyboard. It is used primarily on <input> elements, but is usable on any
// element in contenteditable mode. It's important to understand that the
// inputmode attribute doesn't cause any validity requirements to be enforced on
// input. To require that input conforms to a particular data type, choose an
// appropriate <input> element type. For specific guidance on choosing <input>
// types, see the Values section.
func (e *ObjectElement) Inputmode(c ObjectInputmodeChoice) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("inputmode", string(c))
	return e
}

type ObjectInputmodeChoice string

const (
	// No virtual keyboard. For when the page implements its own keyboard input
	// control.
	ObjectInputmodeNone ObjectInputmodeChoice = "none"
	// Standard input keyboard for the user's current locale.
	ObjectInputmodeEmpty ObjectInputmodeChoice = ""
	// Standard input keyboard for the user's current locale.
	ObjectInputmodeText ObjectInputmodeChoice = "text"
	// Fractional numeric input keyboard containing the digits and decimal separator
	// for the user's locale (typically . or ,). Devices may or may not show a minus
	// key (-).
	ObjectInputmodeDecimal ObjectInputmodeChoice = "decimal"
	// Numeric input keyboard, but only requires the digits 0–9. Devices may or
	// may not show a minus key.
	ObjectInputmodeNumeric ObjectInputmodeChoice = "numeric"
	// A telephone keypad input, including the digits 0–9, the asterisk (*), and
	// the pound (#) key. Inputs that *require* a telephone number should typically
	// use <input type="tel"> instead.
	ObjectInputmodeTel ObjectInputmodeChoice = "tel"
	// A virtual keyboard optimized for search input. For instance, the
	// return/submit key may be labeled "Search", along with possible other
	// optimizations. Inputs that require a search query should typically use <input
	// type="search"> instead.
	ObjectInputmodeSearch ObjectInputmodeChoice = "search"
	// A virtual keyboard optimized for entering email addresses. Typically includes
	// the @character as well as other optimizations. Inputs that require email
	// addresses should typically use <input type="email"> instead.
	ObjectInputmodeEmail ObjectInputmodeChoice = "email"
	// A keypad optimized for entering URLs. This may have the / key more prominent,
	// for example. Enhanced features could include history access and so on. Inputs
	// that require a URL should typically use <input type="url"> instead.
	ObjectInputmodeURL ObjectInputmodeChoice = "url"
)

// The inputmode global attribute is an enumerated attribute that hints at the
// type of data that might be entered by the user while editing the element or
// its contents. This allows a browser to display an appropriate virtual
// keyboard. It is used primarily on <input> elements, but is usable on any
// element in contenteditable mode. It's important to understand that the
// inputmode attribute doesn't cause any validity requirements to be enforced on
// input. To require that input conforms to a particular data type, choose an
// appropriate <input> element type. For specific guidance on choosing <input>
// types, see the Values section.
// Remove the attribute Inputmode from the element.
func (e *ObjectElement) InputmodeRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("inputmode")
	return e
}

// The is global attribute allows you to specify that a standard HTML element
// should behave like a defined custom built-in element (see Using custom
// elements for more details). This attribute can only be used if the specified
// custom element name has been successfully defined in the current document,
// and extends the element type it is being applied to.
func (e *ObjectElement) Is(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("is", s)
	return e
}

// The is global attribute allows you to specify that a standard HTML element
// should behave like a defined custom built-in element (see Using custom
// elements for more details). This attribute can only be used if the specified
// custom element name has been successfully defined in the current document,
// and extends the element type it is being applied to.
func (e *ObjectElement) IsF(format string, args ...any) *ObjectElement {
	return e.Is(fmt.Sprintf(format, args...))
}

// The is global attribute allows you to specify that a standard HTML element
// should behave like a defined custom built-in element (see Using custom
// elements for more details). This attribute can only be used if the specified
// custom element name has been successfully defined in the current document,
// and extends the element type it is being applied to.
func (e *ObjectElement) IfIs(condition bool, s string) *ObjectElement {
	if condition {
		e.Is(s)
	}
	return e
}

// The is global attribute allows you to specify that a standard HTML element
// should behave like a defined custom built-in element (see Using custom
// elements for more details). This attribute can only be used if the specified
// custom element name has been successfully defined in the current document,
// and extends the element type it is being applied to.
func (e *ObjectElement) IfIsF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Is(fmt.Sprintf(format, args...))
	}
	return e
}

// The is global attribute allows you to specify that a standard HTML element
// should behave like a defined custom built-in element (see Using custom
// elements for more details). This attribute can only be used if the specified
// custom element name has been successfully defined in the current document,
// and extends the element type it is being applied to.
// Remove the attribute Is from the element.
func (e *ObjectElement) IsRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("is")
	return e
}

// The itemid global attribute provides microdata in the form of a unique,
// global identifier of an item.
//
// An itemid attribute can only be specified for an element that has both
// itemscope and itemtype attributes. Also, itemid can only be specified on
// elements that possess an itemscope attribute whose corresponding itemtype
// refers to or defines a vocabulary that supports global identifiers. The exact
// meaning of an itemtype's global identifier is provided by the definition of
// that identifier within the specified vocabulary. The vocabulary defines
// whether several items with the same global identifier can coexist and, if so,
// how items with the same identifier are handled.
func (e *ObjectElement) Itemid(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("itemid", s)
	return e
}

// The itemid global attribute provides microdata in the form of a unique,
// global identifier of an item.
//
// An itemid attribute can only be specified for an element that has both
// itemscope and itemtype attributes. Also, itemid can only be specified on
// elements that possess an itemscope attribute whose corresponding itemtype
// refers to or defines a vocabulary that supports global identifiers. The exact
// meaning of an itemtype's global identifier is provided by the definition of
// that identifier within the specified vocabulary. The vocabulary defines
// whether several items with the same global identifier can coexist and, if so,
// how items with the same identifier are handled.
func (e *ObjectElement) ItemidF(format string, args ...any) *ObjectElement {
	return e.Itemid(fmt.Sprintf(format, args...))
}

// The itemid global attribute provides microdata in the form of a unique,
// global identifier of an item.
//
// An itemid attribute can only be specified for an element that has both
// itemscope and itemtype attributes. Also, itemid can only be specified on
// elements that possess an itemscope attribute whose corresponding itemtype
// refers to or defines a vocabulary that supports global identifiers. The exact
// meaning of an itemtype's global identifier is provided by the definition of
// that identifier within the specified vocabulary. The vocabulary defines
// whether several items with the same global identifier can coexist and, if so,
// how items with the same identifier are handled.
func (e *ObjectElement) IfItemid(condition bool, s string) *ObjectElement {
	if condition {
		e.Itemid(s)
	}
	return e
}

// The itemid global attribute provides microdata in the form of a unique,
// global identifier of an item.
//
// An itemid attribute can only be specified for an element that has both
// itemscope and itemtype attributes. Also, itemid can only be specified on
// elements that possess an itemscope attribute whose corresponding itemtype
// refers to or defines a vocabulary that supports global identifiers. The exact
// meaning of an itemtype's global identifier is provided by the definition of
// that identifier within the specified vocabulary. The vocabulary defines
// whether several items with the same global identifier can coexist and, if so,
// how items with the same identifier are handled.
func (e *ObjectElement) IfItemidF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Itemid(fmt.Sprintf(format, args...))
	}
	return e
}

// The itemid global attribute provides microdata in the form of a unique,
// global identifier of an item.
//
// An itemid attribute can only be specified for an element that has both
// itemscope and itemtype attributes. Also, itemid can only be specified on
// elements that possess an itemscope attribute whose corresponding itemtype
// refers to or defines a vocabulary that supports global identifiers. The exact
// meaning of an itemtype's global identifier is provided by the definition of
// that identifier within the specified vocabulary. The vocabulary defines
// whether several items with the same global identifier can coexist and, if so,
// how items with the same identifier are handled.
// Remove the attribute Itemid from the element.
func (e *ObjectElement) ItemidRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("itemid")
	return e
}

// The itemprop global attribute is used to add properties to an item. Every
// HTML element can have an itemprop attribute specified, and an itemprop
// consists of a name-value pair. Each name-value pair is called a property, and
// a group of one or more properties forms an item. Property values are either a
// string or a URL and can be associated with a very wide range of elements
// including <audio>, <embed>, <iframe>, <img>, <link>, <object>, <source>,
// <track>, and <video>.
func (e *ObjectElement) Itemprop(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("itemprop", s)
	return e
}

// The itemprop global attribute is used to add properties to an item. Every
// HTML element can have an itemprop attribute specified, and an itemprop
// consists of a name-value pair. Each name-value pair is called a property, and
// a group of one or more properties forms an item. Property values are either a
// string or a URL and can be associated with a very wide range of elements
// including <audio>, <embed>, <iframe>, <img>, <link>, <object>, <source>,
// <track>, and <video>.
func (e *ObjectElement) ItempropF(format string, args ...any) *ObjectElement {
	return e.Itemprop(fmt.Sprintf(format, args...))
}

// The itemprop global attribute is used to add properties to an item. Every
// HTML element can have an itemprop attribute specified, and an itemprop
// consists of a name-value pair. Each name-value pair is called a property, and
// a group of one or more properties forms an item. Property values are either a
// string or a URL and can be associated with a very wide range of elements
// including <audio>, <embed>, <iframe>, <img>, <link>, <object>, <source>,
// <track>, and <video>.
func (e *ObjectElement) IfItemprop(condition bool, s string) *ObjectElement {
	if condition {
		e.Itemprop(s)
	}
	return e
}

// The itemprop global attribute is used to add properties to an item. Every
// HTML element can have an itemprop attribute specified, and an itemprop
// consists of a name-value pair. Each name-value pair is called a property, and
// a group of one or more properties forms an item. Property values are either a
// string or a URL and can be associated with a very wide range of elements
// including <audio>, <embed>, <iframe>, <img>, <link>, <object>, <source>,
// <track>, and <video>.
func (e *ObjectElement) IfItempropF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Itemprop(fmt.Sprintf(format, args...))
	}
	return e
}

// The itemprop global attribute is used to add properties to an item. Every
// HTML element can have an itemprop attribute specified, and an itemprop
// consists of a name-value pair. Each name-value pair is called a property, and
// a group of one or more properties forms an item. Property values are either a
// string or a URL and can be associated with a very wide range of elements
// including <audio>, <embed>, <iframe>, <img>, <link>, <object>, <source>,
// <track>, and <video>.
// Remove the attribute Itemprop from the element.
func (e *ObjectElement) ItempropRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("itemprop")
	return e
}

// Properties that are not descendants of an element with the itemscope
// attribute can be associated with an item using the global attribute itemref.
// itemref provides a list of element IDs (not itemids) elsewhere in the
// document, with additional properties The itemref attribute can only be
// specified on elements that have an itemscope attribute specified.
func (e *ObjectElement) Itemref(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("itemref", s)
	return e
}

// Properties that are not descendants of an element with the itemscope
// attribute can be associated with an item using the global attribute itemref.
// itemref provides a list of element IDs (not itemids) elsewhere in the
// document, with additional properties The itemref attribute can only be
// specified on elements that have an itemscope attribute specified.
func (e *ObjectElement) ItemrefF(format string, args ...any) *ObjectElement {
	return e.Itemref(fmt.Sprintf(format, args...))
}

// Properties that are not descendants of an element with the itemscope
// attribute can be associated with an item using the global attribute itemref.
// itemref provides a list of element IDs (not itemids) elsewhere in the
// document, with additional properties The itemref attribute can only be
// specified on elements that have an itemscope attribute specified.
func (e *ObjectElement) IfItemref(condition bool, s string) *ObjectElement {
	if condition {
		e.Itemref(s)
	}
	return e
}

// Properties that are not descendants of an element with the itemscope
// attribute can be associated with an item using the global attribute itemref.
// itemref provides a list of element IDs (not itemids) elsewhere in the
// document, with additional properties The itemref attribute can only be
// specified on elements that have an itemscope attribute specified.
func (e *ObjectElement) IfItemrefF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Itemref(fmt.Sprintf(format, args...))
	}
	return e
}

// Properties that are not descendants of an element with the itemscope
// attribute can be associated with an item using the global attribute itemref.
// itemref provides a list of element IDs (not itemids) elsewhere in the
// document, with additional properties The itemref attribute can only be
// specified on elements that have an itemscope attribute specified.
// Remove the attribute Itemref from the element.
func (e *ObjectElement) ItemrefRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("itemref")
	return e
}

// The itemscope global attribute is used to add an item to a microdata DOM
// tree. Every HTML element can have an itemscope attribute specified, and an
// itemscope consists of a name-value pair. Each name-value pair is called a
// property, and a group of one or more properties forms an item. Property
// values are either a string or a URL and can be associated with a very wide
// range of elements including <audio>, <embed>, <iframe>, <img>, <link>,
// <object>, <source>, <track>, and <video>.
func (e *ObjectElement) Itemscope() *ObjectElement {
	e.ItemscopeSet(true)
	return e
}

// The itemscope global attribute is used to add an item to a microdata DOM
// tree. Every HTML element can have an itemscope attribute specified, and an
// itemscope consists of a name-value pair. Each name-value pair is called a
// property, and a group of one or more properties forms an item. Property
// values are either a string or a URL and can be associated with a very wide
// range of elements including <audio>, <embed>, <iframe>, <img>, <link>,
// <object>, <source>, <track>, and <video>.
func (e *ObjectElement) IfItemscope(condition bool) *ObjectElement {
	if condition {
		e.ItemscopeSet(true)
	}
	return e
}

// The itemscope global attribute is used to add an item to a microdata DOM
// tree. Every HTML element can have an itemscope attribute specified, and an
// itemscope consists of a name-value pair. Each name-value pair is called a
// property, and a group of one or more properties forms an item. Property
// values are either a string or a URL and can be associated with a very wide
// range of elements including <audio>, <embed>, <iframe>, <img>, <link>,
// <object>, <source>, <track>, and <video>.
// Set the attribute Itemscope to the value b explicitly.
func (e *ObjectElement) ItemscopeSet(b bool) *ObjectElement {
	if e.BoolAttributes == nil {
		e.BoolAttributes = treemap.New[string, bool]()
	}
	e.BoolAttributes.Set("itemscope", b)
	return e
}

// The itemscope global attribute is used to add an item to a microdata DOM
// tree. Every HTML element can have an itemscope attribute specified, and an
// itemscope consists of a name-value pair. Each name-value pair is called a
// property, and a group of one or more properties forms an item. Property
// values are either a string or a URL and can be associated with a very wide
// range of elements including <audio>, <embed>, <iframe>, <img>, <link>,
// <object>, <source>, <track>, and <video>.
func (e *ObjectElement) IfSetItemscope(condition bool, b bool) *ObjectElement {
	if condition {
		e.ItemscopeSet(b)
	}
	return e
}

// Remove the attribute Itemscope from the element.
// The itemscope global attribute is used to add an item to a microdata DOM
// tree. Every HTML element can have an itemscope attribute specified, and an
// itemscope consists of a name-value pair. Each name-value pair is called a
// property, and a group of one or more properties forms an item. Property
// values are either a string or a URL and can be associated with a very wide
// range of elements including <audio>, <embed>, <iframe>, <img>, <link>,
// <object>, <source>, <track>, and <video>.
func (e *ObjectElement) ItemscopeRemove() *ObjectElement {
	if e.BoolAttributes == nil {
		return e
	}
	e.BoolAttributes.Del("itemscope")
	return e
}

// The itemtype global attribute is used to add types to an item. Every HTML
// element can have an itemtype attribute specified, and an itemtype consists of
// a name-value pair. Each name-value pair is called a property, and a group of
// one or more properties forms an item. Property values are either a string or
// a URL and can be associated with a very wide range of elements including
// <audio>, <embed>, <iframe>, <img>, <link>, <object>, <source>, <track>, and
// <video>.
func (e *ObjectElement) Itemtype(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("itemtype", s)
	return e
}

// The itemtype global attribute is used to add types to an item. Every HTML
// element can have an itemtype attribute specified, and an itemtype consists of
// a name-value pair. Each name-value pair is called a property, and a group of
// one or more properties forms an item. Property values are either a string or
// a URL and can be associated with a very wide range of elements including
// <audio>, <embed>, <iframe>, <img>, <link>, <object>, <source>, <track>, and
// <video>.
func (e *ObjectElement) ItemtypeF(format string, args ...any) *ObjectElement {
	return e.Itemtype(fmt.Sprintf(format, args...))
}

// The itemtype global attribute is used to add types to an item. Every HTML
// element can have an itemtype attribute specified, and an itemtype consists of
// a name-value pair. Each name-value pair is called a property, and a group of
// one or more properties forms an item. Property values are either a string or
// a URL and can be associated with a very wide range of elements including
// <audio>, <embed>, <iframe>, <img>, <link>, <object>, <source>, <track>, and
// <video>.
func (e *ObjectElement) IfItemtype(condition bool, s string) *ObjectElement {
	if condition {
		e.Itemtype(s)
	}
	return e
}

// The itemtype global attribute is used to add types to an item. Every HTML
// element can have an itemtype attribute specified, and an itemtype consists of
// a name-value pair. Each name-value pair is called a property, and a group of
// one or more properties forms an item. Property values are either a string or
// a URL and can be associated with a very wide range of elements including
// <audio>, <embed>, <iframe>, <img>, <link>, <object>, <source>, <track>, and
// <video>.
func (e *ObjectElement) IfItemtypeF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Itemtype(fmt.Sprintf(format, args...))
	}
	return e
}

// The itemtype global attribute is used to add types to an item. Every HTML
// element can have an itemtype attribute specified, and an itemtype consists of
// a name-value pair. Each name-value pair is called a property, and a group of
// one or more properties forms an item. Property values are either a string or
// a URL and can be associated with a very wide range of elements including
// <audio>, <embed>, <iframe>, <img>, <link>, <object>, <source>, <track>, and
// <video>.
// Remove the attribute Itemtype from the element.
func (e *ObjectElement) ItemtypeRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("itemtype")
	return e
}

// The lang global attribute helps define the language of an element: the
// language that non-editable elements are written in or the language that
// editable elements should be written in by the user. The tag contains one
// single entry value in the format defines in the Tags for Identifying
// Languages (BCP47) IETF document. xml:lang has priority over it.
func (e *ObjectElement) Lang(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("lang", s)
	return e
}

// The lang global attribute helps define the language of an element: the
// language that non-editable elements are written in or the language that
// editable elements should be written in by the user. The tag contains one
// single entry value in the format defines in the Tags for Identifying
// Languages (BCP47) IETF document. xml:lang has priority over it.
func (e *ObjectElement) LangF(format string, args ...any) *ObjectElement {
	return e.Lang(fmt.Sprintf(format, args...))
}

// The lang global attribute helps define the language of an element: the
// language that non-editable elements are written in or the language that
// editable elements should be written in by the user. The tag contains one
// single entry value in the format defines in the Tags for Identifying
// Languages (BCP47) IETF document. xml:lang has priority over it.
func (e *ObjectElement) IfLang(condition bool, s string) *ObjectElement {
	if condition {
		e.Lang(s)
	}
	return e
}

// The lang global attribute helps define the language of an element: the
// language that non-editable elements are written in or the language that
// editable elements should be written in by the user. The tag contains one
// single entry value in the format defines in the Tags for Identifying
// Languages (BCP47) IETF document. xml:lang has priority over it.
func (e *ObjectElement) IfLangF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Lang(fmt.Sprintf(format, args...))
	}
	return e
}

// The lang global attribute helps define the language of an element: the
// language that non-editable elements are written in or the language that
// editable elements should be written in by the user. The tag contains one
// single entry value in the format defines in the Tags for Identifying
// Languages (BCP47) IETF document. xml:lang has priority over it.
// Remove the attribute Lang from the element.
func (e *ObjectElement) LangRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("lang")
	return e
}

// The nonce global attribute is a unique identifier used to declare inline
// scripts and style elements to be used in a specific document. It is a
// cryptographic nonce (number used once) that is used by Content Security
// Policy to determine whether or not a given inline script is allowed to
// execute.
func (e *ObjectElement) Nonce(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("nonce", s)
	return e
}

// The nonce global attribute is a unique identifier used to declare inline
// scripts and style elements to be used in a specific document. It is a
// cryptographic nonce (number used once) that is used by Content Security
// Policy to determine whether or not a given inline script is allowed to
// execute.
func (e *ObjectElement) NonceF(format string, args ...any) *ObjectElement {
	return e.Nonce(fmt.Sprintf(format, args...))
}

// The nonce global attribute is a unique identifier used to declare inline
// scripts and style elements to be used in a specific document. It is a
// cryptographic nonce (number used once) that is used by Content Security
// Policy to determine whether or not a given inline script is allowed to
// execute.
func (e *ObjectElement) IfNonce(condition bool, s string) *ObjectElement {
	if condition {
		e.Nonce(s)
	}
	return e
}

// The nonce global attribute is a unique identifier used to declare inline
// scripts and style elements to be used in a specific document. It is a
// cryptographic nonce (number used once) that is used by Content Security
// Policy to determine whether or not a given inline script is allowed to
// execute.
func (e *ObjectElement) IfNonceF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Nonce(fmt.Sprintf(format, args...))
	}
	return e
}

// The nonce global attribute is a unique identifier used to declare inline
// scripts and style elements to be used in a specific document. It is a
// cryptographic nonce (number used once) that is used by Content Security
// Policy to determine whether or not a given inline script is allowed to
// execute.
// Remove the attribute Nonce from the element.
func (e *ObjectElement) NonceRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("nonce")
	return e
}

// The part global attribute contains a space-separated list of the part names
// of the element. Part names allows CSS to select and style specific elements
// in a shadow tree via the ::part pseudo-element.
func (e *ObjectElement) Part(s string) *ObjectElement {
	values := strings.Split(s, " ")
	if e.DelimitedStrings == nil {
		e.DelimitedStrings = treemap.New[string, *DelimitedBuilder[string]]()
	}
	ds, ok := e.DelimitedStrings.Get("part")
	if !ok {
		ds = NewDelimitedBuilder[string](" ")
		e.DelimitedStrings.Set("part", ds)
	}
	ds.Add(values...)
	return e
}

// The part global attribute contains a space-separated list of the part names
// of the element. Part names allows CSS to select and style specific elements
// in a shadow tree via the ::part pseudo-element.
func (e *ObjectElement) IfPart(condition bool, s string) *ObjectElement {
	if condition {
		e.Part(s)
	}
	return e
}

// The part global attribute contains a space-separated list of the part names
// of the element. Part names allows CSS to select and style specific elements
// in a shadow tree via the ::part pseudo-element.
// Remove the values from the attribute Part in the element.
func (e *ObjectElement) PartRemove(s ...string) *ObjectElement {
	if e.DelimitedStrings == nil {
		return e
	}
	ds, ok := e.DelimitedStrings.Get("part")
	if !ok {
		return e
	}
	ds.Remove(s...)
	return e
}

// The popover global attribute is used to designate an element as a popover
// element. Popover elements are hidden via display: none until opened via an
// invoking/control element (i.e. a <button> or <input type="button"> with a
// popovertarget attribute) or a HTMLElement.showPopover() call. When open,
// popover elements will appear above all other elements in the top layer, and
// won't be influenced by parent elements' position or overflow styling.
func (e *ObjectElement) Popover(c ObjectPopoverChoice) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("popover", string(c))
	return e
}

type ObjectPopoverChoice string

const (
	// Popovers that have the auto state can be "light dismissed" by selecting
	// outside the popover area, and generally only allow one popover to be
	// displayed on-screen at a time.
	ObjectPopoverAuto ObjectPopoverChoice = "auto"
	// Popovers that have the auto state can be "light dismissed" by selecting
	// outside the popover area, and generally only allow one popover to be
	// displayed on-screen at a time.
	ObjectPopoverEmpty ObjectPopoverChoice = ""
	// manual popovers must always be explicitly hidden, but allow for use cases
	// such as nested popovers in menus.
	ObjectPopoverManual ObjectPopoverChoice = "manual"
)

// The popover global attribute is used to designate an element as a popover
// element. Popover elements are hidden via display: none until opened via an
// invoking/control element (i.e. a <button> or <input type="button"> with a
// popovertarget attribute) or a HTMLElement.showPopover() call. When open,
// popover elements will appear above all other elements in the top layer, and
// won't be influenced by parent elements' position or overflow styling.
// Remove the attribute Popover from the element.
func (e *ObjectElement) PopoverRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("popover")
	return e
}

// The role global attribute is used to define the purpose or state of an
// element to the browser, in order to facilitate assistive technology such as
// screen readers. It is a simple string value that can be used to describe the
// role of an element.
func (e *ObjectElement) Role(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("role", s)
	return e
}

// The role global attribute is used to define the purpose or state of an
// element to the browser, in order to facilitate assistive technology such as
// screen readers. It is a simple string value that can be used to describe the
// role of an element.
func (e *ObjectElement) RoleF(format string, args ...any) *ObjectElement {
	return e.Role(fmt.Sprintf(format, args...))
}

// The role global attribute is used to define the purpose or state of an
// element to the browser, in order to facilitate assistive technology such as
// screen readers. It is a simple string value that can be used to describe the
// role of an element.
func (e *ObjectElement) IfRole(condition bool, s string) *ObjectElement {
	if condition {
		e.Role(s)
	}
	return e
}

// The role global attribute is used to define the purpose or state of an
// element to the browser, in order to facilitate assistive technology such as
// screen readers. It is a simple string value that can be used to describe the
// role of an element.
func (e *ObjectElement) IfRoleF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Role(fmt.Sprintf(format, args...))
	}
	return e
}

// The role global attribute is used to define the purpose or state of an
// element to the browser, in order to facilitate assistive technology such as
// screen readers. It is a simple string value that can be used to describe the
// role of an element.
// Remove the attribute Role from the element.
func (e *ObjectElement) RoleRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("role")
	return e
}

// The slot global attribute assigns a slot in a shadow DOM shadow tree to an
// element: An element with a slot attribute is assigned to the slot created by
// the <slot> element whose name attribute's value matches that slot attribute's
// value.
func (e *ObjectElement) Slot(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("slot", s)
	return e
}

// The slot global attribute assigns a slot in a shadow DOM shadow tree to an
// element: An element with a slot attribute is assigned to the slot created by
// the <slot> element whose name attribute's value matches that slot attribute's
// value.
func (e *ObjectElement) SlotF(format string, args ...any) *ObjectElement {
	return e.Slot(fmt.Sprintf(format, args...))
}

// The slot global attribute assigns a slot in a shadow DOM shadow tree to an
// element: An element with a slot attribute is assigned to the slot created by
// the <slot> element whose name attribute's value matches that slot attribute's
// value.
func (e *ObjectElement) IfSlot(condition bool, s string) *ObjectElement {
	if condition {
		e.Slot(s)
	}
	return e
}

// The slot global attribute assigns a slot in a shadow DOM shadow tree to an
// element: An element with a slot attribute is assigned to the slot created by
// the <slot> element whose name attribute's value matches that slot attribute's
// value.
func (e *ObjectElement) IfSlotF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Slot(fmt.Sprintf(format, args...))
	}
	return e
}

// The slot global attribute assigns a slot in a shadow DOM shadow tree to an
// element: An element with a slot attribute is assigned to the slot created by
// the <slot> element whose name attribute's value matches that slot attribute's
// value.
// Remove the attribute Slot from the element.
func (e *ObjectElement) SlotRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("slot")
	return e
}

// The spellcheck global attribute is an enumerated attribute that defines
// whether the element may be checked for spelling errors. If this attribute is
// not set, its default value is element-type and browser-defined. This default
// value may also be inherited, which means that the element content will be
// checked for spelling errors only if its nearest ancestor has a spellcheck
// state of true. Security and privacy concerns Using spellchecking can have
// consequences for users' security and privacy. The specification does not
// regulate how spellchecking is done and the content of the element may be sent
// to a third party for spellchecking results (see enhanced spellchecking and
// "spell-jacking"). You should consider setting spellcheck to false for
// elements that can contain sensitive information.
func (e *ObjectElement) Spellcheck(c ObjectSpellcheckChoice) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("spellcheck", string(c))
	return e
}

type ObjectSpellcheckChoice string

const (
	// The element will be checked for spelling errors.
	ObjectSpellcheckEmpty ObjectSpellcheckChoice = ""
	// The element will be checked for spelling errors.
	ObjectSpellcheckTrue ObjectSpellcheckChoice = "true"
	// The element will not be checked for spelling errors.
	ObjectSpellcheckFalse ObjectSpellcheckChoice = "false"
)

// The spellcheck global attribute is an enumerated attribute that defines
// whether the element may be checked for spelling errors. If this attribute is
// not set, its default value is element-type and browser-defined. This default
// value may also be inherited, which means that the element content will be
// checked for spelling errors only if its nearest ancestor has a spellcheck
// state of true. Security and privacy concerns Using spellchecking can have
// consequences for users' security and privacy. The specification does not
// regulate how spellchecking is done and the content of the element may be sent
// to a third party for spellchecking results (see enhanced spellchecking and
// "spell-jacking"). You should consider setting spellcheck to false for
// elements that can contain sensitive information.
// Remove the attribute Spellcheck from the element.
func (e *ObjectElement) SpellcheckRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("spellcheck")
	return e
}

// The style global attribute is used to add styles to an element, such as
// color, font, size, and more. Styles are written in CSS.
func (e *ObjectElement) StylePairs(pairs ...string) *ObjectElement {
	if len(pairs) == 0 || len(pairs)%2 != 0 {
		panic("StylePairs requires an even number of arguments representing key-value pairs.")
	}
	if e.KVStrings == nil {
		e.KVStrings = treemap.New[string, *KVBuilder]()
	}
	kv, ok := e.KVStrings.Get("style")
	if !ok {
		kv = NewKVBuilder(":", ";")
		e.KVStrings.Set("style", kv)
	}
	for i := 0; i < len(pairs)-1; i += 2 {
		key := strings.TrimSpace(pairs[i])
		if key == "" {
			panic("empty key in key-value pairs.")
		}
		value := strings.TrimSpace(pairs[i+1])
		kv.Add(key, value)
	}
	return e
}

// The style global attribute is used to add styles to an element, such as
// color, font, size, and more. Styles are written in CSS.
func (e *ObjectElement) Style(s string) *ObjectElement {
	if e.KVStrings == nil {
		e.KVStrings = treemap.New[string, *KVBuilder]()
	}
	_, ok := e.KVStrings.Get("style")
	if !ok {
		kv := NewKVBuilder(":", ";")
		e.KVStrings.Set("style", kv)
	}
	s = strings.TrimRight(s, ";")
	kvPairs := strings.Split(s, ";")
	for _, pair := range kvPairs {
		parts := strings.SplitN(pair, ":", 2)
		if len(parts) != 2 {
			panic(fmt.Sprintf("invalid key-value pair: %q", pair))
		}
		e.StylePairs(parts[0], parts[1])
	}
	return e
}

// The style global attribute is used to add styles to an element, such as
// color, font, size, and more. Styles are written in CSS.
func (e *ObjectElement) IfStyle(condition bool, s string) *ObjectElement {
	if condition {
		e.Style(s)
	}
	return e
}

// The style global attribute is used to add styles to an element, such as
// color, font, size, and more. Styles are written in CSS.
func (e *ObjectElement) StyleAdd(k string, v string) *ObjectElement {
	if e.KVStrings == nil {
		e.KVStrings = treemap.New[string, *KVBuilder]()
	}
	_, ok := e.KVStrings.Get("style")
	if !ok {
		kv := NewKVBuilder(":", ";")
		e.KVStrings.Set("style", kv)
	}
	e.StylePairs(k, v)
	return e
}

// The style global attribute is used to add styles to an element, such as
// color, font, size, and more. Styles are written in CSS.
func (e *ObjectElement) StyleAddF(k string, format string, args ...any) *ObjectElement {
	return e.StyleAdd(k, fmt.Sprintf(format, args...))
}

// The style global attribute is used to add styles to an element, such as
// color, font, size, and more. Styles are written in CSS.
func (e *ObjectElement) IfStyleAdd(condition bool, k string, v string) *ObjectElement {
	if condition {
		e.StyleAdd(k, v)
	}
	return e
}

// The style global attribute is used to add styles to an element, such as
// color, font, size, and more. Styles are written in CSS.
func (e *ObjectElement) IfStyleAddF(condition bool, k string, format string, args ...any) *ObjectElement {
	if condition {
		e.StyleAddF(k, format, args...)
	}
	return e
}

// The style global attribute is used to add styles to an element, such as
// color, font, size, and more. Styles are written in CSS.
// Add the attributes in the map to the element.
func (e *ObjectElement) StyleMap(m map[string]string) *ObjectElement {
	if e.KVStrings == nil {
		e.KVStrings = treemap.New[string, *KVBuilder]()
	}
	_, ok := e.KVStrings.Get("style")
	if !ok {
		kv := NewKVBuilder(":", ";")
		e.KVStrings.Set("style", kv)
	}
	keys := make([]string, 0, len(m))
	for k := range m {
		keys = append(keys, k)
	}
	sort.Strings(keys)
	for _, k := range keys {
		e.StylePairs(k, m[k])
	}
	return e
}

// The style global attribute is used to add styles to an element, such as
// color, font, size, and more. Styles are written in CSS.
// Remove the attribute Style from the element.
func (e *ObjectElement) StyleRemove(keys ...string) *ObjectElement {
	if e.KVStrings == nil {
		return e
	}
	kv, ok := e.KVStrings.Get("style")
	if !ok {
		return e
	}
	kv.Remove(keys...)
	return e
}

// The tabindex global attribute indicates if its element can be focused, and
// if/where it participates in sequential keyboard navigation (usually with the
// Tab key, hence the name). It accepts an integer as a value, with different
// results depending on the integer's value: a negative value (usually
// tabindex="-1") means that the element should be focusable, but should not be
// reachable via sequential keyboard navigation; a value of 0 (tabindex="0")
// means that the element should be focusable and reachable via sequential
// keyboard navigation, but its relative order is defined by the platform
// convention; a positive value means should be focusable and reachable via
// sequential keyboard navigation; its relative order is defined by the value of
// the attribute: the sequential follow the increasing number of the tabindex.
// If several elements share the same tabindex, their relative order follows
// their relative position in the document.
func (e *ObjectElement) Tabindex(i int) *ObjectElement {
	if e.IntAttributes == nil {
		e.IntAttributes = treemap.New[string, int]()
	}
	e.IntAttributes.Set("tabindex", i)
	return e
}

// The tabindex global attribute indicates if its element can be focused, and
// if/where it participates in sequential keyboard navigation (usually with the
// Tab key, hence the name). It accepts an integer as a value, with different
// results depending on the integer's value: a negative value (usually
// tabindex="-1") means that the element should be focusable, but should not be
// reachable via sequential keyboard navigation; a value of 0 (tabindex="0")
// means that the element should be focusable and reachable via sequential
// keyboard navigation, but its relative order is defined by the platform
// convention; a positive value means should be focusable and reachable via
// sequential keyboard navigation; its relative order is defined by the value of
// the attribute: the sequential follow the increasing number of the tabindex.
// If several elements share the same tabindex, their relative order follows
// their relative position in the document.
func (e *ObjectElement) IfTabindex(condition bool, i int) *ObjectElement {
	if condition {
		e.Tabindex(i)
	}
	return e
}

// The tabindex global attribute indicates if its element can be focused, and
// if/where it participates in sequential keyboard navigation (usually with the
// Tab key, hence the name). It accepts an integer as a value, with different
// results depending on the integer's value: a negative value (usually
// tabindex="-1") means that the element should be focusable, but should not be
// reachable via sequential keyboard navigation; a value of 0 (tabindex="0")
// means that the element should be focusable and reachable via sequential
// keyboard navigation, but its relative order is defined by the platform
// convention; a positive value means should be focusable and reachable via
// sequential keyboard navigation; its relative order is defined by the value of
// the attribute: the sequential follow the increasing number of the tabindex.
// If several elements share the same tabindex, their relative order follows
// their relative position in the document.
// Remove the attribute Tabindex from the element.
func (e *ObjectElement) TabindexRemove() *ObjectElement {
	if e.IntAttributes == nil {
		return e
	}
	e.IntAttributes.Del("tabindex")
	return e
}

// The title global attribute contains text representing advisory information
// related to the element it belongs to. Such information can typically, but not
// necessarily, be presented to the user as a tooltip. The main use of the title
// attribute is to label <iframe> elements for assistive technology. The title
// attribute may also be used to label controls in data tables. The title
// attribute, when added to <link rel="stylesheet">, creates an alternate
// stylesheet. When defining an alternative style sheet with <link
// rel="alternate"> the attribute is required and must be set to a non-empty
// string. If included on the <abbr> opening tag, the title must be a full
// expansion of the abbreviation or acronym. Instead of using title, when
// possible, provide an expansion of the abbreviation or acronym in plain text
// on first use, using the <abbr> to mark up the abbreviation. This enables all
// users know what name or term the abbreviation or acronym shortens while
// providing a hint to user agents on how to announce the content. While title
// can be used to provide a programmatically associated label for an <input>
// element, this is not good practice. Use a <label> instead.
func (e *ObjectElement) Title(s string) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("title", s)
	return e
}

// The title global attribute contains text representing advisory information
// related to the element it belongs to. Such information can typically, but not
// necessarily, be presented to the user as a tooltip. The main use of the title
// attribute is to label <iframe> elements for assistive technology. The title
// attribute may also be used to label controls in data tables. The title
// attribute, when added to <link rel="stylesheet">, creates an alternate
// stylesheet. When defining an alternative style sheet with <link
// rel="alternate"> the attribute is required and must be set to a non-empty
// string. If included on the <abbr> opening tag, the title must be a full
// expansion of the abbreviation or acronym. Instead of using title, when
// possible, provide an expansion of the abbreviation or acronym in plain text
// on first use, using the <abbr> to mark up the abbreviation. This enables all
// users know what name or term the abbreviation or acronym shortens while
// providing a hint to user agents on how to announce the content. While title
// can be used to provide a programmatically associated label for an <input>
// element, this is not good practice. Use a <label> instead.
func (e *ObjectElement) TitleF(format string, args ...any) *ObjectElement {
	return e.Title(fmt.Sprintf(format, args...))
}

// The title global attribute contains text representing advisory information
// related to the element it belongs to. Such information can typically, but not
// necessarily, be presented to the user as a tooltip. The main use of the title
// attribute is to label <iframe> elements for assistive technology. The title
// attribute may also be used to label controls in data tables. The title
// attribute, when added to <link rel="stylesheet">, creates an alternate
// stylesheet. When defining an alternative style sheet with <link
// rel="alternate"> the attribute is required and must be set to a non-empty
// string. If included on the <abbr> opening tag, the title must be a full
// expansion of the abbreviation or acronym. Instead of using title, when
// possible, provide an expansion of the abbreviation or acronym in plain text
// on first use, using the <abbr> to mark up the abbreviation. This enables all
// users know what name or term the abbreviation or acronym shortens while
// providing a hint to user agents on how to announce the content. While title
// can be used to provide a programmatically associated label for an <input>
// element, this is not good practice. Use a <label> instead.
func (e *ObjectElement) IfTitle(condition bool, s string) *ObjectElement {
	if condition {
		e.Title(s)
	}
	return e
}

// The title global attribute contains text representing advisory information
// related to the element it belongs to. Such information can typically, but not
// necessarily, be presented to the user as a tooltip. The main use of the title
// attribute is to label <iframe> elements for assistive technology. The title
// attribute may also be used to label controls in data tables. The title
// attribute, when added to <link rel="stylesheet">, creates an alternate
// stylesheet. When defining an alternative style sheet with <link
// rel="alternate"> the attribute is required and must be set to a non-empty
// string. If included on the <abbr> opening tag, the title must be a full
// expansion of the abbreviation or acronym. Instead of using title, when
// possible, provide an expansion of the abbreviation or acronym in plain text
// on first use, using the <abbr> to mark up the abbreviation. This enables all
// users know what name or term the abbreviation or acronym shortens while
// providing a hint to user agents on how to announce the content. While title
// can be used to provide a programmatically associated label for an <input>
// element, this is not good practice. Use a <label> instead.
func (e *ObjectElement) IfTitleF(condition bool, format string, args ...any) *ObjectElement {
	if condition {
		e.Title(fmt.Sprintf(format, args...))
	}
	return e
}

// The title global attribute contains text representing advisory information
// related to the element it belongs to. Such information can typically, but not
// necessarily, be presented to the user as a tooltip. The main use of the title
// attribute is to label <iframe> elements for assistive technology. The title
// attribute may also be used to label controls in data tables. The title
// attribute, when added to <link rel="stylesheet">, creates an alternate
// stylesheet. When defining an alternative style sheet with <link
// rel="alternate"> the attribute is required and must be set to a non-empty
// string. If included on the <abbr> opening tag, the title must be a full
// expansion of the abbreviation or acronym. Instead of using title, when
// possible, provide an expansion of the abbreviation or acronym in plain text
// on first use, using the <abbr> to mark up the abbreviation. This enables all
// users know what name or term the abbreviation or acronym shortens while
// providing a hint to user agents on how to announce the content. While title
// can be used to provide a programmatically associated label for an <input>
// element, this is not good practice. Use a <label> instead.
// Remove the attribute Title from the element.
func (e *ObjectElement) TitleRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("title")
	return e
}

// The translate global attribute is an enumerated attribute that is used to
// specify whether an element's attribute values and the values of its Text node
// children are to be translated when the page is localized, or whether to leave
// them unchanged.
func (e *ObjectElement) Translate(c ObjectTranslateChoice) *ObjectElement {
	if e.StringAttributes == nil {
		e.StringAttributes = treemap.New[string, string]()
	}
	e.StringAttributes.Set("translate", string(c))
	return e
}

type ObjectTranslateChoice string

const (
	// indicates that the element should be translated when the page is localized.
	ObjectTranslateEmpty ObjectTranslateChoice = ""
	// indicates that the element should be translated when the page is localized.
	ObjectTranslateYes ObjectTranslateChoice = "yes"
	// indicates that the element must not be translated when the page is localized.
	ObjectTranslateNo ObjectTranslateChoice = "no"
)

// The translate global attribute is an enumerated attribute that is used to
// specify whether an element's attribute values and the values of its Text node
// children are to be translated when the page is localized, or whether to leave
// them unchanged.
// Remove the attribute Translate from the element.
func (e *ObjectElement) TranslateRemove() *ObjectElement {
	if e.StringAttributes == nil {
		return e
	}
	e.StringAttributes.Del("translate")
	return e
}
